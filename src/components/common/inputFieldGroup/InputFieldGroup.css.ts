import { style } from "@vanilla-extract/css";

export const inputFieldGroup = style({
  width: "100%",
  display: "flex",
  alignItems: "flex-start",
});

export const inputFieldGroupLabel = style({
  minWidth: "150px",
});
